import 'package:flutter/material.dart';

class InsurancesClinicDetailsScreen extends StatelessWidget {
  const InsurancesClinicDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('INSURANCES');
  }
}
